def explain(query, movie):

    return (
        f"Recommended because "
        f"'{movie['title']}' "
        f"is semantically similar "
        f"to your interest in "
        f"'{query}'."
    )

